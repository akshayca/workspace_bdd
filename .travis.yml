language: java

dist: trusty

addons:
  sonarcloud:
    organization: "paru"
    token:
      secure: ${SONAR_TOKEN}
      

script:
    - mvn clean verify sonar:sonar -Pcoverage -Dsonar.projectKey=workspace_bdd

